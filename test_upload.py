import requests
import json

data = {
    "url": "assets/images/sodium chloride ions.jpeg",
    "storage_type": "url",
    "status": "attached"
}

# we can just test if creating an asset directly works
print("Upload test script created")
